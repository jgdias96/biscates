<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projeto_nav_bar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projeto_nav_bar">
        <activity
            android:name=".Registoconfirmacao"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"></activity>
        <activity
            android:name=".IntroActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegistoActivity" />
        <activity android:name=".LocalidadeActivity" />
        <activity android:name=".ConfregistoActivity" />
        <activity android:name=".SenhaActivity" />
        <activity android:name=".EmailActivity" />
        <activity android:name=".MoradaActivity" />
        <activity android:name=".PagamentoActivity" />
        <activity android:name=".PromocaoActivity" />
        <activity android:name=".PromokActivity" />
        <activity android:name=".ChatActivity" />
    </application>

</manifest>